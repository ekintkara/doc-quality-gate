# Doc Quality Gate Integration

This project uses Doc Quality Gate (DQG) to review implementation documents against the codebase before coding begins.

## What DQG Does

DQG analyzes an implementation document (feature spec, implementation plan, architecture change, etc.) and cross-references it with the actual codebase to find:

- **Missing APIs** — Document references endpoints/routes that don't exist
- **Missing Models** — Document references data models/schemas not in the codebase
- **Missing Dependencies** — Required libraries not in package.json/requirements.txt
- **Conflicting Design** — Proposed changes conflict with current architecture
- **Redundant Work** — Document proposes building something that already exists
- **Architecture Mismatch** — Doesn't follow the project's established patterns
- **Quality Issues** — Contradictions, incomplete logic, missing requirements, edge cases

## How to Use

### Option 1: Jira Task'tan Implementasyon Dökümanı Üret + Analiz

Bir Jira task'ından otomatik implementasyon dökümanı üret ve DQG analizi yap:

```
/dqg from-jira PDB-11139 --cp C:\OBTaskManager\obiletcontext
```

Proje cross-reference ile:
```
/dqg from-jira PDB-11139 --cp C:\OBTaskManager\obiletcontext --project C:\obilet-core-v2
```

Sadece döküman üret (analiz yapmadan):
```
/dqg from-jira PDB-11139 --cp C:\OBTaskManager\obiletcontext --generate-only
```

**Jira akışı:**
1. Jira task'ı okunur (ADF açıklaması parse edilir, yorumlar alınır)
2. Task netlik/kalite analizi yapılır (clarity score)
3. LLM ile implementasyon dökümanı üretilir
4. DQG pipeline'ı çalışır (critic → validate → score)
5. Score < 8.0 ise düzeltme aksiyon planı sunulur
6. Düzeltmeler uygulanıp tekrar çalıştırılır
7. Score >= 8.0 ve kullanıcı onayı → döküman hazır

### Option 2: Mevcut Dökümanı Analiz Et

When you create an implementation document, run:

```
/dqg path/to/document.md
```

With context path:
```
/dqg path/to/document.md --cp C:\OBTaskManager\obiletcontext
```

Or let DQG find the most recent document:

```
/dqg
```

### Option 3: Direct CLI

**macOS / Linux:**
```bash
cd /path/to/doc-quality-gate
source .venv/bin/activate
python -m app.cli review path/to/document.md --project /path/to/target/project
```

**Windows:**
```powershell
cd C:\path\to\doc-quality-gate
.venv\Scripts\Activate.ps1
python -m app.cli review path\to\document.md --project C:\path\to\target\project
```

**Jira task'tan (Windows):**
```powershell
.venv\Scripts\Activate.ps1
python -m app.cli from-jira PDB-11139 --cp C:\OBTaskManager\obiletcontext
```

### Option 4: Wrapper Script

**Windows:**
```powershell
powershell -File doc-quality-gate/scripts/win/dqg-review.ps1 path\to\document.md
```

**macOS / Linux:**
```bash
bash doc-quality-gate/scripts/mac/dqg-review.sh path/to/document.md
```

## Prerequisites

Run the setup script for your platform — it installs dependencies, configures `.env`, and starts the LiteLLM proxy:

| Platform | Double-click | Terminal |
|----------|-------------|----------|
| Windows | `scripts/win/setup.bat` | `powershell -File scripts/win/setup.ps1` |
| macOS | `scripts/mac/setup.command` | `bash scripts/mac/setup.sh` |
| Linux | — | `bash scripts/linux/setup.sh` |

### Jira Integration Prerequisites

For `from-jira` command, add these to your `.env`:

```
DQG_JIRA_BASE_URL=https://obilet.atlassian.net
DQG_JIRA_EMAIL=your.email@obilet.com
DQG_JIRA_API_TOKEN=your-api-token
DQG_JIRA_PROJECT=PDB
DQG_JIRA_DEFAULT_CONTEXT_PATH=C:\OBTaskManager\obiletcontext
```

## After Review

When `/dqg` returns results:

1. **Read the cross-reference issues first** — these show mismatches between the document and this codebase
2. **Fix the document** — Update the implementation document based on findings
3. **Re-run if needed** — `/dqg` again to verify fixes
4. **Implement** — Only start coding after the document passes the quality gate (score >= 8.0)

### Jira Task Iterative Loop

After `from-jira` returns with score < 8.0:

1. Review `report.md` in the output directory
2. Fix the implementation document
3. Re-run: `/dqg outputs/jira-PDB-XXXX-impl-plan.md --cp C:\OBTaskManager\obiletcontext`
4. Repeat until score >= 8.0

## Workflow Guidelines

- Always create an implementation document before starting significant coding work
- Use `/dqg` to validate the document against the codebase
- Use `/dqg from-jira PDB-XXXX --cp C:\OBTaskManager\obiletcontext` to generate implementation documents from Jira tasks
- Address critical and high-severity issues before proceeding
- The revised document generated by DQG can be used as the implementation reference
- If DQG score is below 8.0, iterate on the document before implementing
